Copia vendoreada de `Portfolio/shared/tutorial/`.

Se vendorea a proposito: cada demo es un repo hermano autocontenido (regla de
estructura del brief), asi que un `import` que suba fuera del repo romperia el
clon. Si cambia el upstream, recopiar estos dos archivos.

Se sirven desde `public/`, de modo que la ruta publica lleva el `base` del
sitio: `/botanica/tutorial/tutorial.js`.
